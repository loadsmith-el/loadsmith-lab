---
description: Create a new lab source-service image (Dockerfile + init + smoke case)
argument-hint: <service> <version> [data-ref] [notes]
allowed-tools: [Read, Write, Bash]
---

The user invoked this command with: $ARGUMENTS

Read and follow the full instructions in
[`.agents/skills/create-source-image.md`](../../.agents/skills/create-source-image.md).
The real, agent-agnostic logic lives there so the knowledge base stays
centralized in `.agents/`.
